source "https://rubygems.org"

# Matches the versions GitHub Pages builds with
gem "github-pages", group: :jekyll_plugins

gem "webrick", "~> 1.8"

group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
end
